name: install Apache on ec2 host
yum:
    name:httpd
    stat:latest

name:start apache service
service:
    name:httpd
    state:started

name:enable httpd service
service:
    name:httpd
    state:enabled
